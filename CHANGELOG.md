# 更新记录

## 未发布

- 移除外部聊天应用入口，双击重新完全用于桌宠互动。
- 设置面板增加“休息片刻”，复用现有自然作息状态。
- 收紧首页说明，删除重复的开发过程描述。

## 0.9.0-rc.3  2026-09-06

- 修正音效调用中的 .NET 命名空间错误，改用 `System.Media.SystemSounds`；
  根据互动类型选择系统音，不在播放时重复加载程序集。
- 面板和托盘的音效、自启设置复用同一组动作，避免闭包作用域中的状态错位，
  同步两个入口的勾选状态。自启检查核对当前安装路径。
- 绑定托盘退出动作，保存状态后关闭程序。
- 扩展现有自检，实际触发托盘音效点击、面板点击和托盘退出；移除未验证功能
  的硬编码通过标记。测试模式和持久化探针不播放提示音。
- 增加 GitHub Windows 检查、源码排除规则和干净运行包。
